name: Test SSH Connection

on: 
  workflow_dispatch: 

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test SSH Connection
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 91.160.65.250
          username: deploy
          key:  ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            echo "✅ SSH connection successful!"
            whoami
            pwd
            ls -la /var/www/ri/SAE-GROUPE-14
